services:
  webcam:
    build: .
    ports:
      - "8443:8443"
    volumes:
      - ./certs:/app/certs:ro
    environment:
      - PORT=8443
      - CERT_PATH=/app/certs/local.crt
      - KEY_PATH=/app/certs/local.key

services:
  webcam:
    build: .
    ports:
      - "8443:8443"
      - "8080:8080"
    volumes:
      - ./certs:/app/certs:ro
    environment:
      - PORT=8443
      - HTTP_PORT=8080
      - CERT_PATH=/app/certs/local.crt
      - KEY_PATH=/app/certs/local.key
